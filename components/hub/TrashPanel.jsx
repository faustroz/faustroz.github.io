"use client";
import { useCallback, useEffect, useState } from "react";
import { RotateCcw, Trash2 } from "lucide-react";
import { requireSupabase } from "@/lib/supabase/client";
const tables = [["expenses","Expenses"],["income_entries","Income"],["financial_goals","Goals"],["bank_accounts","Balances"],["finance_categories","Categories"],["budgets","Budgets"],["subscriptions","Subscriptions"],["study_topics","Topics"],["study_exams","Exams"],["study_flashcards","Flashcards"],["academic_records","Academic"],["hub_projects","Projects"],["project_tasks","Tasks"],["project_changelog","Changelog"],["ai_memory_entries","Memory"],["vault_documents","Vault"]];
const title = (row) => row.title || row.name || row.course_name || row.file_name || row.front || "Private record";
export default function TrashPanel() { const [items,setItems]=useState([]); const [error,setError]=useState(""); const load=useCallback(async()=>{try{const client=requireSupabase();const groups=await Promise.all(tables.map(async([table,label])=>{const {data,error}=await client.from(table).select("*").not("deleted_at","is",null).order("deleted_at",{ascending:false});if(error)throw error;return(data||[]).map(row=>({table,label,row}));}));setItems(groups.flat().sort((a,b)=>new Date(b.row.deleted_at)-new Date(a.row.deleted_at)));}catch(e){setError(e.message);}},[]);useEffect(()=>{load();},[load]);const restore=async(item)=>{const {error}=await requireSupabase().from(item.table).update({deleted_at:null}).eq("id",item.row.id);if(error)return setError(error.message);load();};const remove=async(item)=>{if(!window.confirm("Permanently delete this record? This cannot be undone."))return;const client=requireSupabase();if(item.table==="vault_documents")await client.storage.from("document-vault").remove([item.row.storage_path]);const {error}=await client.from(item.table).delete().eq("id",item.row.id);if(error)return setError(error.message);load();};return <section className="hub-crud-panel"><header className="hub-crud-heading"><div><span>RECOVERY / 30 DAYS</span><h2>Trash</h2><p>Deleted Hub records can be restored for 30 days. After that, the cleanup job permanently removes them.</p></div></header>{error&&<p className="hub-data-error">{error}</p>}{items.length?<div className="hub-record-grid">{items.map((item,index)=><article className="hub-record" key={`${item.table}-${item.row.id}`}><div className="hub-record-index">{String(index+1).padStart(2,"0")}</div><div className="hub-record-values"><div><span>MODULE</span><strong>{item.label}</strong></div><div><span>RECORD</span><strong>{title(item.row)}</strong></div><div><span>EXPIRES</span><strong>{new Date(new Date(item.row.deleted_at).getTime()+30*864e5).toLocaleDateString("id-ID")}</strong></div></div><div className="hub-record-actions"><button type="button" onClick={()=>restore(item)} aria-label="Restore"><RotateCcw/></button><button type="button" onClick={()=>remove(item)} aria-label="Delete permanently"><Trash2/></button></div></article>)}</div>:<div className="hub-data-state">TRASH EMPTY / RECOVERY READY</div>}</section>; }
